#!/usr/bin/env node
var debug = require('debug')('shedsModelQueue');
var app = require('../app');

app.set('port', 1001);


var server = app.listen(app.get('port'), function() {
  console.log("Listening on port: " + server.address().port);
  debug('Express server listening on port ' + server.address().port);
});
