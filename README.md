shedsModelQueue
===============

The model queue for the SHEDS USGS project

## Installation
1. Run ```npm install```
2. Install *redis-server* with ```apt-get install redis-server```
3. Run ```npm start```